/**
 * chat-group-routes.ts — Group info, add member, friends list endpoints.
 * Extracted from chat-routes.ts for modularization.
 */
import type { FastifyInstance } from 'fastify';
export declare function chatGroupRoutes(app: FastifyInstance): Promise<void>;
